# Runbook fuerza bruta

Revisar eventos `login_failed`, `rate_limit_triggered` y cuentas bloqueadas. Confirmar IP hasheada, ventanas y usuario objetivo. Mantener mensajes genéricos sin enumerar usuarios.
