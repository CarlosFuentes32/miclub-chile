# CORS y headers

CORS usa allowlist explícita. `*.vercel.app` solo se permite con `ALLOW_VERCEL_PREVIEWS=true`. Headers activos: CSP básica, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, X-Frame-Options, COOP y HSTS en producción.
