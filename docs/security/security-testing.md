# Pruebas de seguridad

Ejecutar `npm run test:enterprise-security`, `test:user-reactivation`, `test:enterprise-logs`, Playwright staging y audit. Cubren rate limit, CSRF/origin, headers, refresh reuse, webhooks, auditoría y sesiones.
