import { ArrowLeft,ShieldCheck } from 'lucide-react';import { QRCodeSVG } from 'qrcode.react';import { useNavigate } from 'react-router-dom';import { CustomerProfile } from '../types/customer';
export function QRScreen({profile,token}:{profile:CustomerProfile;token:string}){const navigate=useNavigate();return <main className="min-h-screen bg-slate-950 px-5 pb-10 pt-[max(1.25rem,env(safe-area-inset-top))] text-white"><div className="mx-auto max-w-md"><button onClick={()=>navigate(-1)} className="grid h-11 w-11 place-items-center rounded-full bg-white/10" aria-label="Volver"><ArrowLeft/></button><header className="mt-6 text-center"><p className="text-sm font-semibold uppercase tracking-[.2em] text-violet-300">MiClub Chile</p><h1 className="mt-2 text-3xl font-black">Mi QR</h1></header><section className="mt-7 rounded-[2rem] bg-white p-6 text-center text-slate-950 shadow-2xl"><div className="mx-auto w-fit rounded-3xl border-8 border-violet-50 p-3"><QRCodeSVG value={token} size={238} level="M" marginSize={1}/></div><h2 className="mt-5 text-2xl font-black">{profile.name}</h2><p className="mt-1 text-sm text-slate-500">Código de respaldo</p><p className="mt-2 font-mono text-2xl font-black tracking-[.18em] text-violet-700">{profile.shortCode}</p></section><div className="mt-6 flex gap-3 rounded-2xl bg-white/10 p-4 text-sm leading-relaxed text-slate-200"><ShieldCheck className="shrink-0 text-violet-300"/><p>Muestra este código al cajero para registrar tu compra o canjear una recompensa.</p></div></div></main>}
