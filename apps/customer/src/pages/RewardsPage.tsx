import { useState } from 'react';import { RewardCard } from '../components/RewardCard';import { Reward,RewardStatus } from '../types/customer';
const tabs:{id:RewardStatus;label:string}[]=[{id:'available',label:'Disponibles'},{id:'used',label:'Utilizadas'},{id:'expired',label:'Vencidas'}];
export function RewardsPage({rewards}:{rewards:Reward[]}){const[tab,setTab]=useState<RewardStatus>('available');const visible=rewards.filter(r=>r.status===tab);return <main className="px-5 pt-[max(1.5rem,env(safe-area-inset-top))]"><p className="text-sm font-bold text-violet-600">Tus beneficios</p><h1 className="mt-1 text-3xl font-black">Mis recompensas</h1><div className="mt-6 grid grid-cols-3 rounded-2xl bg-slate-100 p-1">{tabs.map(item=><button key={item.id} onClick={()=>setTab(item.id)} className={`rounded-xl px-2 py-3 text-xs font-bold ${tab===item.id?'bg-white text-violet-700 shadow-sm':'text-slate-500'}`}>{item.label}</button>)}</div><div className="mt-5 space-y-3">{visible.length?visible.map(r=><RewardCard key={r.id} reward={r}/>):<p className="rounded-2xl bg-white p-8 text-center text-sm text-slate-500">No hay recompensas en esta categoría.</p>}</div></main>}
