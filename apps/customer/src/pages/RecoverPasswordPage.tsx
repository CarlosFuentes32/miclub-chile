import{FormEvent,useState}from'react';import{ArrowLeft}from'lucide-react';import{Link}from'react-router-dom';import{customerService}from'../services/customer.service';
export function RecoverPasswordPage(){const[value,setValue]=useState(''),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);try{const r=await customerService.requestPasswordReset(value);setMessage(r.message)}finally{setBusy(false)}}return <main className="min-h-screen bg-white p-6"><Link to="/login" className="grid h-11 w-11 place-items-center rounded-full bg-slate-100"><ArrowLeft/></Link><form onSubmit={submit} className="mx-auto mt-10 max-w-sm"><img src="/logo-miclub-chile.jpeg" alt="MiClub Chile" className="mx-auto h-28 w-auto rounded-2xl object-contain"/><h1 className="mt-6 text-3xl font-black">Recuperar contraseña</h1><p className="mt-2 text-slate-500">Ingresa tu correo electrónico o teléfono.</p><label className="mt-6 block text-sm font-semibold">Correo o teléfono<input className="input" value={value} onChange={e=>setValue(e.target.value)} required/></label>{message&&<p role="status" className="mt-4 rounded-2xl bg-cyan-50 p-4 text-sm font-semibold text-cyan-900">{message}</p>}<button disabled={busy} className="mt-5 w-full rounded-2xl bg-gradient-to-r from-cyan-500 to-violet-700 py-4 font-bold text-white">{busy?'Enviando…':'Solicitar recuperación'}</button></form></main>}
