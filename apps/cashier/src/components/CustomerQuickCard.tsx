import { RotateCcw,ShoppingBag } from 'lucide-react';import { CashierCustomer } from '../types/cashier';import { ProgressDisplay } from './ProgressDisplay';import { RewardAvailableCard } from './RewardAvailableCard';
export function CustomerQuickCard({customer,onRegister,onCancel,busy=false,showRegister=true}:{customer:CashierCustomer;onRegister?:()=>void;onCancel?:()=>void;busy?:boolean;showRegister?:boolean}){const available=customer.rewards.filter(r=>r.status==='available');const canCancel=customer.lastTransaction?.status==='completed'&&new Date(customer.lastTransaction.cancellableUntil)>new Date();return <section className="rounded-3xl bg-white p-5 shadow-xl shadow-slate-200/70"><div className="flex items-center gap-3"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-amber-100 text-xl font-black text-amber-800">{customer.name.charAt(0)}</span><div><h2 className="text-xl font-black">{customer.name}</h2><p className="text-sm text-slate-500">{customer.business}</p></div></div><div className="mt-5"><ProgressDisplay current={customer.current} goal={customer.goal}/></div><div className="mt-4 rounded-2xl bg-slate-50 p-4"><p className="text-xs font-bold uppercase tracking-wide text-slate-400">Próxima recompensa</p><p className="mt-1 font-black">{customer.nextReward}</p></div>{available.length>0&&<div className="mt-4 space-y-2">{available.map(reward=><RewardAvailableCard key={reward.id} reward={reward}/>)}</div>}{showRegister&&<button onClick={onRegister} disabled={busy} className="mt-5 flex min-h-16 w-full items-center justify-center gap-2 rounded-2xl bg-slate-950 px-4 text-lg font-black text-white disabled:opacity-50"><ShoppingBag/> {busy?'Registrando…':'Registrar transacción'}</button>}{canCancel&&onCancel&&<button onClick={onCancel} disabled={busy} className="mt-2 flex w-full items-center justify-center gap-2 rounded-2xl py-3 text-sm font-bold text-red-600"><RotateCcw size={17}/> Anular última transacción</button>}</section>}
