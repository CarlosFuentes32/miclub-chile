export function ProgressDisplay({current,goal}:{current:number;goal:number}){return <div><div className="flex items-end justify-between"><p className="text-sm font-semibold text-slate-500">Progreso actual</p><p className="font-black text-slate-900">{current} de {goal}</p></div><div className="mt-2 h-3 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-violet-600 transition-all" style={{width:`${Math.min(100,current/goal*100)}%`}}/></div><p className="mt-2 text-sm text-slate-500">Faltan <strong className="text-slate-800">{Math.max(goal-current,0)}</strong> para la recompensa</p></div>}
