import { Search } from 'lucide-react';
export function PhoneSearchInput({digits,onChange,searching=false}:{digits:string;onChange:(digits:string)=>void;searching?:boolean}){function update(value:string){onChange(value.replace(/\D/g,'').slice(-8))}return <div><label className="text-sm font-bold text-slate-700" htmlFor="phone-search">Número de teléfono</label><div className="mt-2 flex min-h-16 items-center rounded-2xl border-2 border-slate-200 bg-white px-4 focus-within:border-amber-500 focus-within:ring-4 focus-within:ring-amber-100"><Search className="mr-3 shrink-0 text-slate-400"/><span className="font-black text-slate-700">+569</span><input id="phone-search" autoFocus inputMode="numeric" pattern="[0-9]*" autoComplete="tel" value={digits} onChange={e=>update(e.target.value)} placeholder="95026368" className="min-w-0 flex-1 bg-transparent py-4 text-xl font-black tracking-wide outline-none" aria-describedby="phone-help"/>{searching&&<span className="h-5 w-5 animate-spin rounded-full border-2 border-amber-500 border-t-transparent"/>}</div><p id="phone-help" className="mt-2 text-sm text-slate-500">Escribe solo los 8 dígitos finales. Buscaremos automáticamente.</p>{digits&&<p className="mt-3 text-center text-2xl font-black text-slate-950">+569{digits}</p>}</div>}
