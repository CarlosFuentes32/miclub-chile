import { AuthUser } from '@miclub/shared';import { CashierHomeActions } from '../components/CashierHomeActions';
export function HomePage({user,onLogout}:{user:AuthUser;onLogout:()=>void}){return <main className="mx-auto min-h-screen max-w-md bg-slate-50 px-5 pb-8 pt-[max(1.5rem,env(safe-area-inset-top))]"><header className="mb-6"><p className="text-sm font-black uppercase tracking-wider text-amber-600">Café Central · Caja</p><h1 className="mt-1 text-3xl font-black">Hola, {user.name.split(' ')[0]}</h1><p className="mt-1 text-slate-500">¿Qué necesitas hacer?</p></header><CashierHomeActions onLogout={onLogout}/></main>}
