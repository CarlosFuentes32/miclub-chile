import { useRef,useState } from 'react';import { Copy,Download,Printer } from 'lucide-react';import { QRCodeCanvas } from 'qrcode.react';import { QRMaterial } from '../types/commerce';
export function QRMaterialCard({material}:{material:QRMaterial}){const canvasRef=useRef<HTMLCanvasElement>(null),[copied,setCopied]=useState(false);function download(){const link=document.createElement('a');link.download=`qr-inscripcion-${material.businessCode}.png`;link.href=canvasRef.current?.toDataURL('image/png')??'';link.click()}async function copy(){await navigator.clipboard.writeText(material.registrationUrl);setCopied(true)}return <div className="grid min-w-0 gap-5 lg:grid-cols-[1fr_.8fr]"><section className="min-w-0 rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="mx-auto w-fit max-w-full rounded-3xl border-8 border-violet-50 p-4"><QRCodeCanvas ref={canvasRef} value={material.registrationUrl} size={230} level="H" marginSize={2}/></div><h2 className="mt-5 text-center text-xl font-black">QR de inscripción</h2><p className="mt-1 break-words text-center font-mono font-bold text-violet-700">{material.businessCode}</p><div className="mt-5 rounded-2xl bg-amber-50 p-4 text-sm font-semibold text-amber-900">Este QR solo sirve para registrar nuevos clientes. No suma puntos, sellos ni recompensas.</div><div className="mt-4 flex flex-col gap-3 sm:flex-row"><button onClick={download} className="primary flex-1 justify-center"><Download size={18}/> Descargar QR</button><button onClick={copy} className="secondary flex-1 justify-center"><Copy size={18}/> {copied?'Copiado':'Copiar link'}</button></div><p className="mt-4 break-all text-center text-xs text-slate-500">{material.registrationUrl}</p></section><section className="poster min-w-0 rounded-3xl bg-slate-950 p-6 text-center text-white shadow-xl sm:p-8"><p className="text-sm font-bold uppercase tracking-[.2em] text-cyan-300">MiClub Chile</p><h2 className="mt-5 text-3xl font-black">Únete al club de<br/>{material.businessName}</h2><div className="mx-auto mt-7 w-fit max-w-full rounded-3xl bg-white p-4"><QRCodeCanvas value={material.registrationUrl} size={190}/></div><p className="mt-6 text-lg font-bold">Escanea y empieza a recibir beneficios</p><button onClick={()=>window.print()} className="mt-6 inline-flex max-w-full items-center justify-center gap-2 rounded-2xl bg-white px-5 py-3 font-bold text-slate-950 print:hidden"><Printer size={18}/> Imprimir afiche</button></section></div>}
