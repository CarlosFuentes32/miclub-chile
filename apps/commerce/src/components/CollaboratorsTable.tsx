import { Collaborator } from '../types/commerce';
export function CollaboratorsTable({items,onEdit,onToggle}:{items:Collaborator[];onEdit:(c:Collaborator)=>void;onToggle:(c:Collaborator)=>void}){return <div className="overflow-hidden rounded-3xl border border-slate-200 bg-white">{items.map(c=><div key={c.id} className="flex flex-col gap-3 border-t border-slate-100 p-5 first:border-0 sm:flex-row sm:items-center"><div className="flex-1"><strong>{c.name}</strong><p className="text-sm text-slate-500">{c.email}</p></div><span className="w-fit rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{c.role==='CASHIER'?'Cajero':'Administrador'}</span><span className={`w-fit rounded-full px-3 py-1 text-xs font-bold ${c.status==='active'?'bg-emerald-50 text-emerald-700':'bg-red-50 text-red-600'}`}>{c.status==='active'?'Activo':'Inactivo'}</span><div className="flex gap-2"><button onClick={()=>onEdit(c)} className="secondary">Editar</button><button onClick={()=>onToggle(c)} className="secondary">{c.status==='active'?'Desactivar':'Activar'}</button></div></div>)}</div>}
