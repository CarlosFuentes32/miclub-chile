import { LoyaltyProgram,LoyaltyProgramDraft,ProgramType } from '../types/commerce';
const names:Record<ProgramType,string>={stamps:'Sellos',points:'Puntos',cashback:'Cashback',visits:'Visitas',amount:'Monto acumulado'};
export function ProgramSummaryCard({program}:{program:LoyaltyProgram|LoyaltyProgramDraft}){const expiry=program.validity==='none'?'Sin vencimiento':program.validity==='custom'?program.customExpiry:`${program.validity} días`;return <div className="divide-y divide-slate-100 rounded-3xl border border-slate-200 bg-white p-5 shadow-sm">{[['Tipo de programa',names[program.type]],['Regla de acumulación',program.earningRule],['Meta del ciclo',program.goalLabel],['Recompensa',program.reward],['Vigencia',expiry]].map(([k,v])=><div key={k} className="py-3 first:pt-0 last:pb-0"><p className="text-xs font-bold uppercase tracking-wide text-slate-400">{k}</p><p className="mt-1 font-bold text-slate-900">{v}</p></div>)}</div>}
