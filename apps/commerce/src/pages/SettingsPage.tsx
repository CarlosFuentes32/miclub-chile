import { LogOut } from 'lucide-react';import { BusinessSettingsForm } from '../components/BusinessSettingsForm';import { commerceService } from '../services/commerce.service';import { BusinessSettings } from '../types/commerce';export function SettingsPage({settings,onChange,onLogout}:{settings:BusinessSettings;onChange:(s:BusinessSettings)=>void;onLogout:()=>void}){async function save(v:BusinessSettings){onChange(await commerceService.updateBusinessSettings(v))}return <div className="page"><p className="eyebrow">Configuración básica</p><h1 className="title">Ajustes del negocio</h1><p className="subtitle">Mantén actualizada la información que ven tus clientes.</p><div className="mt-7 rounded-3xl border border-slate-200 bg-white p-5 md:p-7"><BusinessSettingsForm settings={settings} onSave={save}/></div><button onClick={onLogout} className="mt-5 flex items-center gap-2 font-bold text-red-600"><LogOut size={18}/> Cerrar sesión</button></div>}
