import { FormEvent,useEffect,useState } from 'react';import { Plus } from 'lucide-react';import { CollaboratorsTable } from '../components/CollaboratorsTable';import { commerceService } from '../services/commerce.service';import { Collaborator } from '../types/commerce';
export function CollaboratorsPage(){const[items,setItems]=useState<Collaborator[]>([]),[open,setOpen]=useState(false),[editing,setEditing]=useState<Collaborator|null>(null),[name,setName]=useState(''),[email,setEmail]=useState(''),[role,setRole]=useState<Collaborator['role']>('CASHIER');useEffect(()=>{commerceService.getCollaborators().then(setItems)},[]);function edit(c:Collaborator){setEditing(c);setName(c.name);setEmail(c.email);setRole(c.role);setOpen(true)}async function submit(e:FormEvent){e.preventDefault();if(editing){const updated=await commerceService.updateCollaborator({...editing,name,email,role});setItems(c=>c.map(x=>x.id===updated.id?updated:x))}else{const created=await commerceService.createCollaborator({name,email,role});setItems(c=>[created,...c])}setOpen(false);setEditing(null);setName('');setEmail('')}async function toggle(c:Collaborator){const updated=await commerceService.updateCollaborator({...c,status:c.status==='active'?'inactive':'active'});setItems(v=>v.map(x=>x.id===updated.id?updated:x))}return <div className="page"><div className="flex items-start justify-between gap-4"><div><p className="eyebrow">Equipo del comercio</p><h1 className="title">Colaboradores</h1><p className="subtitle">Administra accesos sin exponer configuración a cajeros.</p></div><button onClick={()=>setOpen(true)} className="primary shrink-0"><Plus size={18}/> Crear colaborador</button></div><div className="mt-7"><CollaboratorsTable items={items} onEdit={edit} onToggle={toggle}/></div>{open&&<div className="fixed inset-0 z-50 grid place-items-end bg-slate-950/40 p-3 sm:place-items-center" onClick={()=>setOpen(false)}><form onSubmit={submit} onClick={e=>e.stopPropagation()} className="w-full max-w-md rounded-3xl bg-white p-6"><h2 className="text-2xl font-black">{editing?'Editar colaborador':'Nuevo colaborador'}</h2><label className="field mt-5">Nombre<input className="input" value={name} onChange={e=>setName(e.target.value)} required/></label><label className="field mt-4">Correo<input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="field mt-4">Rol<select className="input" value={role} onChange={e=>setRole(e.target.value as Collaborator['role'])}><option value="CASHIER">Cajero</option><option value="BUSINESS_ADMIN">Administrador de comercio</option></select></label><button className="primary mt-5 w-full">Guardar colaborador</button></form></div>}</div>}
