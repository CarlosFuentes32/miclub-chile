import { SupportTicketsTable } from '../components/SupportTicketsTable';import { SupportTicket } from '../types/admin';export function SupportPage({items}:{items:SupportTicket[]}){return <div className="page"><p className="eyebrow">Estructura inicial</p><h1 className="title">Soporte</h1><p className="subtitle">Vista de tickets preparada para una implementación futura.</p><div className="mt-7"><SupportTicketsTable items={items}/></div></div>}
