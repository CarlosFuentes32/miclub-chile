import { GlobalSettingsPanel } from '../components/GlobalSettingsPanel';import { adminService } from '../services/admin.service';import { GlobalSettings } from '../types/admin';export function SettingsPage({settings,onChange}:{settings:GlobalSettings;onChange:(s:GlobalSettings)=>void}){async function save(v:GlobalSettings){onChange(await adminService.updateGlobalSettings(v))}return <div className="page"><p className="eyebrow">Listas base</p><h1 className="title">Configuración global</h1><p className="subtitle">Administra catálogos y textos comunes de la plataforma.</p><div className="mt-7 rounded-3xl border border-slate-200 bg-white p-6"><GlobalSettingsPanel settings={settings} onSave={save}/></div><p className="mt-5 rounded-2xl bg-slate-100 p-4 text-sm text-slate-600">Las auditorías son de solo lectura y no se modifican desde este panel.</p></div>}
