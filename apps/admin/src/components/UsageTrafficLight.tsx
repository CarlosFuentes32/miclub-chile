import { UsageLevel } from '../types/admin';const data:Record<UsageLevel,[string,string]>={frequent:['bg-emerald-500','Uso frecuente'],low:['bg-amber-500','Bajo uso'],inactive:['bg-red-500','Sin uso reciente']};export function UsageTrafficLight({level}:{level:UsageLevel}){return <span className="inline-flex items-center gap-2 text-xs font-bold text-slate-600"><i className={`h-3 w-3 rounded-full ${data[level][0]}`}/>{data[level][1]}</span>}
