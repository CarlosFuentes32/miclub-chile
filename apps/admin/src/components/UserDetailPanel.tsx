import { X } from 'lucide-react';import { AdminUser,UserStatus } from '../types/admin';import { StatusBadge } from './StatusBadge';export function UserDetailPanel({user,onClose,onStatus}:{user:AdminUser;onClose:()=>void;onStatus:(s:UserStatus)=>void}){return <div className="fixed inset-0 z-50 flex justify-end bg-slate-950/40" onClick={onClose}><aside onClick={e=>e.stopPropagation()} className="h-full w-full max-w-lg bg-white p-6"><button onClick={onClose} className="float-right grid h-10 w-10 place-items-center rounded-full bg-slate-100"><X/></button><p className="eyebrow">Detalle de usuario</p><h2 className="mt-2 text-3xl font-black">{user.name}</h2><div className="mt-3"><StatusBadge status={user.status}/></div><dl className="mt-7 space-y-4">{[['Correo',user.email],['Teléfono',user.phone],['Rol',user.role],['Comercio',user.business??'Sin comercio'],['Registro',user.createdAt],['Último acceso',user.lastAccess]].map(([k,v])=><div key={k} className="rounded-2xl bg-slate-50 p-4"><dt className="text-xs font-bold text-slate-400">{k}</dt><dd className="mt-1 font-bold">{v}</dd></div>)}</dl><button onClick={()=>onStatus(user.status==='active'?'suspended':'active')} className={`mt-7 w-full ${user.status==='active'?'danger':'primary'}`}>{user.status==='active'?'Suspender usuario':'Activar usuario'}</button></aside></div>}
