import { AlertTriangle,Building2,Gift,ReceiptText,Users,WalletCards } from 'lucide-react';import { AdminDashboard } from '../types/admin';
export function AdminDashboardCards({data}:{data:AdminDashboard}){const cards=[['Comercios activos',data.activeBusinesses,Building2],['Usuarios registrados',data.registeredUsers,Users],['Transacciones hoy',data.todayTransactions,ReceiptText],['Recompensas generadas',data.rewardsGenerated,Gift],['Comercios en riesgo',data.atRiskBusinesses,AlertTriangle],['Ingresos mensuales',`$${data.estimatedMonthlyRevenue.toLocaleString('es-CL')}`,WalletCards]] as const;return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{cards.map(([label,value,Icon])=><article key={label} className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex justify-between"><p className="text-sm font-bold text-slate-500">{label}</p><Icon size={20} className="text-violet-600"/></div><p className="mt-4 text-3xl font-black">{value}</p></article>)}</div>}
