import { changeOwnPassword } from '@miclub/shared';
import { FormEvent, useState } from 'react';

export function ForcePasswordChange({ onComplete }: { onComplete: () => void | Promise<void> }) {
  const [password, setPassword] = useState(''), [confirm, setConfirm] = useState(''), [message, setMessage] = useState(''), [saving, setSaving] = useState(false);
  async function submit(event: FormEvent) { event.preventDefault(); if (password.length < 8) return setMessage('La contraseña debe tener al menos 8 caracteres.'); if (password !== confirm) return setMessage('Las contraseñas no coinciden.'); setSaving(true); setMessage(''); try { await changeOwnPassword(password); await onComplete(); } catch (error) { setMessage(error instanceof Error ? error.message : 'No fue posible cambiar la contraseña.'); } finally { setSaving(false); } }
  return <main className="grid min-h-screen place-items-center bg-slate-950 p-5 text-slate-950"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-7 shadow-2xl"><p className="text-sm font-black uppercase tracking-widest text-violet-600">Seguridad MiClub</p><h1 className="mt-2 text-3xl font-black">Debes crear una nueva contraseña antes de continuar.</h1><p className="mt-3 text-sm leading-6 text-slate-500">La clave temporal solo permitió verificar tu identidad. Elige una contraseña privada de al menos 8 caracteres.</p><label className="mt-6 block text-sm font-bold">Nueva contraseña<input className="mt-2 w-full rounded-2xl border border-slate-200 px-4 py-3.5" type="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8}/></label><label className="mt-4 block text-sm font-bold">Confirmar contraseña<input className="mt-2 w-full rounded-2xl border border-slate-200 px-4 py-3.5" type="password" autoComplete="new-password" value={confirm} onChange={e=>setConfirm(e.target.value)} required minLength={8}/></label>{message&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-sm font-bold text-red-700">{message}</p>}<button disabled={saving} className="mt-6 w-full rounded-2xl bg-gradient-to-r from-cyan-500 to-violet-600 py-4 font-black text-white disabled:opacity-50">{saving?'Guardando…':'Guardar nueva contraseña'}</button></form></main>;
}
